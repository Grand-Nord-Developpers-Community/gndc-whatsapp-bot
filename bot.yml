# =====================
# WhatsApp Bot Settings
# =====================
bot:
  name: "GNDC Bot" # The display name of your bot
  description: "Je suis le bot de la GNDC, prêt pour servir dans l'objectif de rendre cette communauté actif via \n https://gndc.tech"
  online: true # Should the bot appear online?
  prefix: ">" # Command prefix (e.g., !, >, /)
  history: false # Enable/disable chat history

# =====================
# Logging
# =====================
logging:
  level: info # Log level: error, warn, info, debug
  logToFile: true # Save logs to file

# =====================
# Advanced/Experimental
# =====================
# Add more config options as needed
